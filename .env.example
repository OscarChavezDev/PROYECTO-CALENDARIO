# Frontend-safe Supabase values
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only values. Nunca exponer en frontend.
SUPABASE_SERVICE_ROLE_KEY=

# Web Push / VAPID
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Email / SMTP fallback
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=a